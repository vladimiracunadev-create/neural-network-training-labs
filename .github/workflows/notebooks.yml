name: Notebook and assessment contracts

on:
  pull_request:
    paths:
      - 'labs/**'
      - 'advanced_labs/**'
      - 'assignments/**'
      - 'src/**'
      - 'configs/**'
      - 'nbgrader_config.py'
      - 'scripts/generate_specialized_notebooks.py'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -e '.[dev,notebooks,education]'
      - run: neural-labs validate --warnings-as-errors
      - run: python scripts/validate_repository.py
      - run: pytest tests/test_repository_structure.py tests/test_schema_v2.py tests/test_notebook_specialization_v3.py -q
      - name: Validate nbgrader metadata
        run: python scripts/validate_nbgrader.py
      # Un ejercicio cuya solución de referencia no ejecuta es peor que no
      # tenerlo, así que aquí se ejecutan las 328 celdas evaluables.
      - name: Los cinco ejercicios de cada laboratorio, con su solución
        run: pytest tests/test_notebook_exercises_v3.py -q
      - name: Los cuadernos con ejercicios están al día
        run: python scripts/generate_advanced_exercises.py --check
